#include<stdio.h>
#include<math.h>
void main() {
    int l,b,result;
    printf("enter the length and breath:");
    scanf("%d%d",&l,&b);
    result=2*(l+b);
    printf("the perameter of rectangle is:%d",result);
}
